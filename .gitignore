# Secrets — never commit
.env
*.pem
.aws/

# Model weights and HF cache — these live on a Docker volume, never in git
models/
*.safetensors
*.bin
*.gguf

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Generated sample media (recreate with scripts/make_assets.py)
assets/*.jpg
assets/*.mp4

# Load test output
loadtest/results/
*.json.gz

# Phase 2 scratch — written by docs/EC2.md so docs/TEARDOWN.md can read back what
# was created. Account-specific, and never useful to anyone cloning the repo.
.bucket-name
.instance-info

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
